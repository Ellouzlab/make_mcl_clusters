package:
  name: mobile_clusters
  version: 0.1.2

source:
  path: .

build:
  noarch: python
  script: "python -m pip install -e ."

requirements:
  host:
    - python >=3.10
    - pip
    - build
    - setuptools
    - setuptools-scm
  run:
    - python >=3.10
    - pandas >=2.0.0
    - biopython >=1.84
    - tqdm >=2.0.0
    - matplotlib >=3.0.0
    - diamond >=2.0.0
    - mmseqs2 >=15.6
    - mcl >=22-282


about:
  homepage: https://github.com/Ellouzlab/make_mcl_clusters  # Replace with the URL of your package's repository
  license: MIT  # Replace with your package's license
  summary: "A package to create MCL clusters"
  description: |
    A package to create MCL clusters.
  repository: https://github.com/Ellouzlab/make_mcl_clusters  # Replace with the URL for development-related information

extra:
  recipe-maintainers:
    - sulmanm1  # Replace with your GitHub username
